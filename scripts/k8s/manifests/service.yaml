apiVersion: v1
kind: Service
metadata:
  name: Durar
  labels:
    app: Durar
spec:
  type: ClusterIP
  selector:
    app: Durar
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
