services:
  Durar-gateway:
    image: ${Durar_IMAGE:-Durar:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      Durar_GATEWAY_TOKEN: ${Durar_GATEWAY_TOKEN:-}
      Durar_ALLOW_INSECURE_PRIVATE_WS: ${Durar_ALLOW_INSECURE_PRIVATE_WS:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${Durar_TZ:-UTC}
    volumes:
      - ${Durar_CONFIG_DIR}:/home/node/.Durar
      - ${Durar_WORKSPACE_DIR}:/home/node/.Durar/workspace
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg Durar_INSTALL_DOCKER_CLI=1) or use
      ## scripts/docker/setup.sh with Durar_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    ports:
      - "${Durar_GATEWAY_PORT:-18789}:18789"
      - "${Durar_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${Durar_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  Durar-cli:
    image: ${Durar_IMAGE:-Durar:local}
    network_mode: "service:Durar-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      Durar_GATEWAY_TOKEN: ${Durar_GATEWAY_TOKEN:-}
      Durar_ALLOW_INSECURE_PRIVATE_WS: ${Durar_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${Durar_TZ:-UTC}
    volumes:
      - ${Durar_CONFIG_DIR}:/home/node/.Durar
      - ${Durar_WORKSPACE_DIR}:/home/node/.Durar/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - Durar-gateway
